Model artefacts are generated by running Notebook 07. They are excluded from
version control because of file size. After running the notebook you will find:

    tfidf_vectoriser.pkl
    label_encoder.pkl
    best_model_Logistic_Regression.pkl
    distilbert/  (generated by Notebook 08 if trained)

For team collaboration, upload these files to shared cloud storage
(Google Drive, S3, Azure Blob) and document the download path here.
